# v1.4 변경사항

## AI 모델 선택
- `OPENAI_API_KEY`는 계속 Vercel 서버 환경변수에 안전하게 보관합니다.
- `OPENAI_MODEL` 환경변수는 더 이상 사용하지 않습니다.
- 관리자 업로드 화면에서 GPT-5.6 Luna / Terra / Sol 중 하나를 선택합니다.
- 선택한 값은 브라우저 로컬 저장소에 저장되어 다음 업로드에도 유지됩니다.

## 성적표 삭제
- 학생별 개별 성적표 삭제
- 같은 엑셀 업로드 묶음 전체 삭제
- 저장된 전체 성적표와 링크 삭제
- 삭제 시 Supabase 데이터도 제거되며 기존 공개 링크는 즉시 무효화됩니다.
- 마지막 학생이 삭제되면 빈 `report_batches` 레코드도 자동 정리됩니다.

추가 Supabase SQL 실행은 필요하지 않습니다.
