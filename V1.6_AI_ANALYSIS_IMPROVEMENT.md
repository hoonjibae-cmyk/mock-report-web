# v1.6 AI 분석·표시 개선

- 학년 값이 `중3` 또는 `3`으로 입력되어도 화면에는 항상 `중3`으로 한 번만 표시
- AI 입력 데이터를 학부모가 이해하기 쉬운 광역 학습 영역으로 재분류
- `닮음·내심` 등 세부 전문 용어를 그대로 나열하지 않고 `삼각형·도형의 성질과 공간 추론`처럼 설명
- 권장 학습 계획을 `여름방학 학습 로드맵`으로 변경
- 단기 문제 수 지시 대신 중3 기초 점검 → 취약 영역 보완 → 고등 과정 연결 순서로 안내
- 수학은 중3 기초가 부족한 경우 과도한 선행보다 개념 복습 우선
- 영어는 점수와 취약 영역에 따라 어휘·문장 구조·정확한 해석을 문제풀이보다 우선 안내
- AI가 생성한 로드맵은 과목별 데이터 기반 규칙으로 한 번 더 검증해 이상한 단기 계획을 방지
- 오답 문항 셀, 표 행, 오답 배지를 진한 빨간색으로 강화

기존 데이터베이스 구조 변경은 없습니다. 새 AI 총평 문구는 성적표를 새로 생성할 때 적용됩니다.
